{
  "name": "shelnet-wiki",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "node scripts/build-manifest.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "minisearch": "^7.1.0"
  }
}
